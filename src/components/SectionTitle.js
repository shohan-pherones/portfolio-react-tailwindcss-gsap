const SectionTitle = ({ title }) => {
  return (
    <span className="section-title text-4xl text-gray-400 font-light mb-10 inline-block">
      {title}
    </span>
  );
};

export default SectionTitle;
